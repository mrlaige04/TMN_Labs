Міністерство освіти і науки України
Національний технічний університет України «КПІ імені Ігоря Сікорського»
Факультет інформатики та обчислювальної техніки
Кафедра інформаційних систем та технологій
ЗВІТ
з лабораторної роботи № 2
з курсу «Технології машинного навчання»
Тема: «Large Language Models + Retrieval Augmented Generation»
Перевірив:
викл.
Сарнацький Владислав Віталійович
Виконав:
Студент групи ІА-51мп
Рудяков Ілля
Київ 2026
Лабораторна робота №2
Тема: Large Language Models + Retrieval Augmented Generation.
Хід роботи
Аналогічно до попередньої лабораторної роботи будемо розділяти код на різні директорії для створення архітектури проекту.
Почнемо з найжничого рівня (база даних). Для зручності роботи з базою даних була використана ORM бібліотека SQL Alchemy. В файлі database.py ми створюємо сесію (механізм роботи з базою даних).
Далі в окремому файлі створимо модель (схему) таблиці бази даних DocumentChunk. Ключовими атрибутами є ідентифікатор чанку, індекс чанку, джерело і контент (текст+ембедінги):
Також для зручності використання з'єднання з базою даних та виконання запитів, було створено додаткові методи для основних операцій (вставка, отримання):
Перейдемо до основної логіки. Важливим файлом є settings.py який задає конфіг для роботи проекту (строка підключення до бази даних, модель для створення ембедінгів та розмірність)
У файлі chunking помістимо метод який буде розбивати вхідний текст (контент документу) на чанки:
І у файлі Embeddings.py завантажуємо модель Sentence Transformer для перетворення чанків на вектори (ембедінги):
Далі у директорії api створимо ендпоінти для роботи з додатком. Перший метод для завантаження документу, обробки, трансформації та збереження у базу даних.
І другий метод для створення запитів (на вхід приймає вектор) і повертає топ N найближчих чанків.
І головний файл, де запускаємо весь додаток:
Для зручності запуску додатку, було створено інфраструкуру для запуску в Docker. 
Запустивши компоуз файл отримали запущені контейнери для бази даних та для АПІ:
Для тесту створимо простий скрипт який буде завантажувати локальні .txt файли, а потім отримувати input від юзера і відправляти на /query ендпоінт.
Результат роботи:
Перейдемо до завдання 2. В роботі використаємо LLM Ollama, яку будемо запускати в тому ж docker compose в якості окремого контейнера. 
Для роботи з LLM створимо окрему АПІ в піддиректорії llm-api.
В даному АПІ будемо використовувати попередню базу даних, яку заповнили даними про документи, завантаженні в минулому завданні. Для цього повністю повторимо схему роботи з бд:
Перейдемо до моделей (класів). Вони включають запит від користувача, чанк та відповідь:
В prompting.py задамо такий промпт для LLM:
І створимо метод для роботи з самою LLM:
Інші файли повторюють логіку з минулого завдання.
В докер компоузі додано два нових сервіси:
Можемо протестувати роботу:
Трохи змінимо та вдосконалимо промпт:
Як бачимо, покращивши промпт для LLM, відповідь стає чіткішою та набагато кращою.
Висновки: .